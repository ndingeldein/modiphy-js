define(['core/view'], function(View){
	'use strict';

	var SingleViewer = ItemView.extend({

		initialize: function(){
			this.listenTo(this.collection, 'select:one', this.)
		}

	});

	return ItemView;

});